# Example Environment Variables for Whiz Pro Bot (soon to be WHIZ-MD)
#
# Copy this file to a new file named .env in the same directory (whiz-pro-bot/)
# and fill in your actual session ID.
#
# IMPORTANT: Add the .env file to your .gitignore file to prevent committing secrets!
# Example .gitignore line:
# .env

# Get this session ID from the Whiz Session Generator web application.
# It's the long JSON string that the generator provides (without the "WHIZBOT_" prefix).
WHATSAPP_SESSION_ID=""
